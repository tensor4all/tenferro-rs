schema = "tenferro.storage-ownership-contracts.v2"

[registry]

[[registry.gates]]
id = "G1"
title = "G1 storage ownership gate"

[[registry.gates]]
id = "G2"
title = "G2 storage ownership gate"

[[registry.gates]]
id = "G3"
title = "G3 storage ownership gate"

[[registry.gates]]
id = "G4"
title = "G4 storage ownership gate"

[[registry.gates]]
id = "G5"
title = "G5 storage ownership gate"

[[registry.gates]]
id = "G6"
title = "G6 storage ownership gate"

[[registry.gates]]
id = "G7"
title = "G7 storage ownership gate"

[[registry.units]]
id = "P0"
issue = 1556
name = "control-plane"

[[registry.units]]
id = "P1"
issue = 1557
name = "contract-ledger"

[[registry.units]]
id = "P2"
issue = 1558
name = "root-claims"

[[registry.units]]
id = "P3"
issue = 1559
name = "host-ownership"

[[registry.units]]
id = "P4"
issue = 1560
name = "access-retirement"

[[registry.units]]
id = "P5"
issue = 1561
name = "allocation-group"

[[registry.units]]
id = "P6"
issue = 1562
name = "reinterpret"

[[registry.units]]
id = "P7"
issue = 1563
name = "cuda"

[[registry.units]]
id = "P8"
issue = 1564
name = "webgpu-metal"

[[registry.units]]
id = "P9"
issue = 1565
name = "runtime-ad-cutover"

[[registry.units]]
id = "P10"
issue = 1566
name = "api-normalization"

[[registry.units]]
id = "P11"
issue = 1568
name = "hardware"

[[registry.units]]
id = "P12"
issue = 1569
name = "documentation"

[[registry.units]]
id = "P13-A"
issue = 1567
name = "freeze"

[[registry.units]]
id = "P13-B"
issue = 1567
name = "closure"

[[registry.edges]]
from = "P1"
to = "P2"

[[registry.edges]]
from = "P2"
to = "P4"

[[registry.edges]]
from = "P4"
to = "P5"

[[registry.edges]]
from = "P3"
to = "P6"

[[registry.edges]]
from = "P9"
to = "P6"

[[registry.edges]]
from = "P6"
to = "P7"

[[registry.edges]]
from = "P6"
to = "P8"

[[registry.edges]]
from = "P7"
to = "P10"

[[registry.edges]]
from = "P8"
to = "P10"

[[registry.edges]]
from = "P10"
to = "P13-A"

[[registry.edges]]
from = "P13-A"
to = "P11"

[[registry.edges]]
from = "P13-A"
to = "P12"

[[registry.edges]]
from = "P11"
to = "P13-B"

[[registry.edges]]
from = "P12"
to = "P13-B"

[[registry.cohorts]]
id = "cutover"
prerequisites = ["P0", "P5"]
members = ["P3", "P9"]

[[obligations]]
id = "p1-ledger"
unit = "P1"
gates = ["G1", "G3", "G5"]
artifact = { id = "artifact-ledger", kind = "manifest", path = "scripts/storage-ownership-contracts.toml" }
command = { id = "cmd-ledger", kind = "python-test", argv = ["python3", "scripts/check-storage-ownership-contracts.py"], cwd = ".", path_args = ["scripts/check-storage-ownership-contracts.py", "scripts/storage-ownership-contracts.toml"], artifact_id = "artifact-ledger" }
state = { kind = "active" }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p1-contract-document"
unit = "P1"
gates = ["G6"]
artifact = { id = "artifact-contract-document", kind = "documentation", path = "docs/design/storage-ownership-contracts.md" }
command = { id = "cmd-contract-document", kind = "doc-check", argv = ["python3", "scripts/check-storage-design-docs.py"], cwd = ".", path_args = ["scripts/check-storage-design-docs.py", "docs/design/storage-ownership-contracts.md"], artifact_id = "artifact-contract-document" }
state = { kind = "active" }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p1-api-parity"
unit = "P1"
gates = ["G4"]
artifact = { id = "artifact-api-parity", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_api_parity.rs" }
command = { id = "cmd-api-parity", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_api_parity"], cwd = ".", path_args = [], artifact_id = "artifact-api-parity" }
state = { kind = "active" }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p0-control-plane"
unit = "P0"
gates = ["G3"]
artifact = { id = "artifact-control-plane", kind = "rust-test", path = "crates/tenferro-runtime/tests/execution_engine_identity.rs" }
command = { id = "cmd-control-plane", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-runtime", "--test", "execution_engine_identity"], cwd = ".", path_args = [], artifact_id = "artifact-control-plane" }
state = { kind = "deferred", activation_unit = "P0", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p1-element-access-baseline"
unit = "P1"
gates = ["G4"]
artifact = { id = "artifact-element-access-baseline", kind = "benchmark-report", path = "docs/testing/storage-element-access-baseline.json" }
command = { id = "cmd-element-access-baseline", kind = "benchmark-check", argv = ["python3", "scripts/verify-storage-element-access-baseline.py", "--report", "docs/testing/storage-element-access-baseline.json"], cwd = ".", path_args = ["scripts/verify-storage-element-access-baseline.py", "docs/testing/storage-element-access-baseline.json"], artifact_id = "artifact-element-access-baseline" }
state = { kind = "active" }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p2-root-claims"
unit = "P2"
gates = ["G1"]
artifact = { id = "artifact-root-claims", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_root_claims.rs" }
command = { id = "cmd-root-claims", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_root_claims"], cwd = ".", path_args = [], artifact_id = "artifact-root-claims" }
state = { kind = "deferred", activation_unit = "P2", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p4-production-borrow-contract"
unit = "P4"
gates = ["G1", "G4"]
artifact = { id = "artifact-production-borrow-contract", kind = "compile-contract", path = "crates/tenferro-tensor/tests/storage_borrow_contract.rs" }
command = { id = "cmd-production-borrow-contract", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_borrow_contract"], cwd = ".", path_args = [], artifact_id = "artifact-production-borrow-contract" }
state = { kind = "deferred", activation_unit = "P4", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p3-host-owner"
unit = "P3"
gates = ["G4"]
artifact = { id = "artifact-owner-compile", kind = "trybuild", path = "crates/tenferro-tensor/tests/ui/storage/fail/owned_storage_not_clone.rs" }
command = { id = "cmd-owner-compile", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_compile_contract"], cwd = ".", path_args = [], artifact_id = "artifact-owner-compile" }
state = { kind = "deferred", activation_unit = "P3", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p3-static-rank-preservation"
unit = "P3"
gates = ["G4"]
artifact = { id = "artifact-static-rank-preservation", kind = "compile-contract", path = "crates/tenferro-tensor/tests/storage_static_rank.rs" }
command = { id = "cmd-static-rank-preservation", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_static_rank"], cwd = ".", path_args = [], artifact_id = "artifact-static-rank-preservation" }
state = { kind = "deferred", activation_unit = "P3", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p3-as-view-zero-allocation"
unit = "P3"
gates = ["G1", "G4"]
artifact = { id = "artifact-as-view-zero-allocation", kind = "allocation-borrow-contract", path = "crates/tenferro-tensor/tests/storage_as_view_allocation.rs" }
command = { id = "cmd-as-view-zero-allocation", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_as_view_allocation"], cwd = ".", path_args = [], artifact_id = "artifact-as-view-zero-allocation" }
state = { kind = "deferred", activation_unit = "P3", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p3-auto-trait-contract"
unit = "P3"
gates = ["G1", "G4"]
artifact = { id = "artifact-storage-auto-traits", kind = "compile-contract", path = "crates/tenferro-tensor/tests/storage_auto_traits.rs" }
command = { id = "cmd-storage-auto-traits", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_auto_traits"], cwd = ".", path_args = [], artifact_id = "artifact-storage-auto-traits" }
state = { kind = "deferred", activation_unit = "P3", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p4-access-retirement"
unit = "P4"
gates = ["G1", "G3"]
artifact = { id = "artifact-prepared-validation-boundary", kind = "validation-boundary-test", path = "crates/tenferro-tensor/tests/storage_prepared_validation.rs" }
command = { id = "cmd-prepared-validation-boundary", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_prepared_validation"], cwd = ".", path_args = [], artifact_id = "artifact-prepared-validation-boundary" }
state = { kind = "deferred", activation_unit = "P4", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p4-provider-release-lifecycle"
unit = "P4"
gates = ["G1", "G3"]
artifact = { id = "artifact-provider-event-retirement", kind = "provider-test", path = "crates/tenferro-tensor/tests/storage_provider_event_retirement.rs" }
command = { id = "cmd-provider-event-retirement", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_provider_event_retirement"], cwd = ".", path_args = [], artifact_id = "artifact-provider-event-retirement" }
state = { kind = "deferred", activation_unit = "P4", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p4-traversal-resolution-counts"
unit = "P4"
gates = ["G1", "G3", "G4"]
artifact = { id = "artifact-traversal-resolution-counts", kind = "provider-test", path = "crates/tenferro-tensor/tests/storage_traversal_resolution.rs" }
command = { id = "cmd-traversal-resolution-counts", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_traversal_resolution"], cwd = ".", path_args = [], artifact_id = "artifact-traversal-resolution-counts" }
state = { kind = "deferred", activation_unit = "P4", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p4-prepared-access-api"
unit = "P4"
gates = ["G1", "G4"]
artifact = { id = "artifact-prepared-access-api", kind = "api-source-contract", path = "crates/tenferro-tensor/tests/storage_prepared_access.rs" }
command = { id = "cmd-prepared-access-api", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_prepared_access"], cwd = ".", path_args = [], artifact_id = "artifact-prepared-access-api" }
state = { kind = "deferred", activation_unit = "P4", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p5-allocation-group"
unit = "P5"
gates = ["G2", "G5"]
artifact = { id = "artifact-allocation-group", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_allocation_group.rs" }
command = { id = "cmd-allocation-group", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_allocation_group"], cwd = ".", path_args = [], artifact_id = "artifact-allocation-group" }
state = { kind = "deferred", activation_unit = "P5", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p9-submission"
unit = "P9"
gates = ["G2", "G3", "G7"]
artifact = { id = "artifact-submit-compile", kind = "trybuild", path = "crates/tenferro-tensor/tests/ui/storage/pass/consuming_submission.rs" }
command = { id = "cmd-submit-compile", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_compile_contract"], cwd = ".", path_args = [], artifact_id = "artifact-submit-compile" }
state = { kind = "deferred", activation_unit = "P9", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p6-reinterpret"
unit = "P6"
gates = ["G1", "G2", "G4"]
artifact = { id = "artifact-reinterpret", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_reinterpret.rs" }
command = { id = "cmd-reinterpret", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_reinterpret"], cwd = ".", path_args = [], artifact_id = "artifact-reinterpret" }
state = { kind = "deferred", activation_unit = "P6", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p6-reinterpret-rank-policy"
unit = "P6"
gates = ["G4"]
artifact = { id = "artifact-reinterpret-rank-policy", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_reinterpret_rank.rs" }
command = { id = "cmd-reinterpret-rank-policy", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_reinterpret_rank"], cwd = ".", path_args = [], artifact_id = "artifact-reinterpret-rank-policy" }
state = { kind = "deferred", activation_unit = "P6", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p7-cuda"
unit = "P7"
gates = ["G1", "G3", "G5"]
artifact = { id = "artifact-cuda-provider", kind = "hardware-test", path = "crates/tenferro-cuda/tests/storage_provider.rs" }
command = { id = "cmd-cuda-provider", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-cuda", "--test", "storage_provider"], cwd = ".", path_args = [], artifact_id = "artifact-cuda-provider" }
state = { kind = "deferred", activation_unit = "P7", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p8-webgpu-metal"
unit = "P8"
gates = ["G1", "G3", "G5"]
artifact = { id = "artifact-webgpu-metal-provider", kind = "hardware-test", path = "crates/tenferro-wgpu/tests/storage_provider.rs" }
command = { id = "cmd-webgpu-metal-provider", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-wgpu", "--test", "storage_provider"], cwd = ".", path_args = [], artifact_id = "artifact-webgpu-metal-provider" }
state = { kind = "deferred", activation_unit = "P8", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p10-api-normalization"
unit = "P10"
gates = ["G4"]
artifact = { id = "artifact-api-normalization", kind = "rust-test", path = "crates/tenferro-tensor/tests/storage_public_api.rs" }
command = { id = "cmd-api-normalization", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tensor", "--test", "storage_public_api"], cwd = ".", path_args = [], artifact_id = "artifact-api-normalization" }
state = { kind = "deferred", activation_unit = "P10", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p10-element-hot-path-structure"
unit = "P10"
gates = ["G1", "G4"]
artifact = { id = "artifact-element-hot-path-structure", kind = "source-contract", path = "scripts/check-storage-element-hot-path.py" }
command = { id = "cmd-element-hot-path-structure", kind = "python-test", argv = ["python3", "scripts/check-storage-element-hot-path.py"], cwd = ".", path_args = ["scripts/check-storage-element-hot-path.py"], artifact_id = "artifact-element-hot-path-structure" }
state = { kind = "deferred", activation_unit = "P10", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p10-storage-traversal-performance"
unit = "P10"
gates = ["G1", "G4"]
artifact = { id = "artifact-storage-traversal-performance", kind = "performance-report", path = "docs/testing/storage-traversal-performance.md" }
command = { id = "cmd-storage-traversal-performance", kind = "benchmark-check", argv = ["python3", "scripts/verify-storage-traversal-performance.py", "--baseline-obligation", "p1-element-access-baseline", "--baseline-report", "docs/testing/storage-element-access-baseline.json", "--report", "docs/testing/storage-traversal-performance.md"], cwd = ".", path_args = ["scripts/verify-storage-traversal-performance.py", "docs/testing/storage-element-access-baseline.json", "docs/testing/storage-traversal-performance.md"], artifact_id = "artifact-storage-traversal-performance" }
state = { kind = "deferred", activation_unit = "P10", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p10-static-rank-codegen"
unit = "P10"
gates = ["G4"]
artifact = { id = "artifact-static-rank-codegen", kind = "codegen-report", path = "docs/testing/storage-static-rank-codegen.md" }
command = { id = "cmd-static-rank-codegen", kind = "codegen-check", argv = ["python3", "scripts/check-storage-static-rank-codegen.py", "--report", "docs/testing/storage-static-rank-codegen.md"], cwd = ".", path_args = ["scripts/check-storage-static-rank-codegen.py", "docs/testing/storage-static-rank-codegen.md"], artifact_id = "artifact-static-rank-codegen" }
state = { kind = "deferred", activation_unit = "P10", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p13-freeze"
unit = "P13-A"
gates = ["G1", "G2", "G3", "G4", "G5", "G6", "G7"]
artifact = { id = "artifact-contract-freeze", kind = "documentation", path = "docs/design/storage-contract-freeze.md" }
command = { id = "cmd-contract-freeze", kind = "doc-check", argv = ["python3", "scripts/check-storage-contract-freeze.py", "--report", "docs/design/storage-contract-freeze.md"], cwd = ".", path_args = ["scripts/check-storage-contract-freeze.py", "docs/design/storage-contract-freeze.md"], artifact_id = "artifact-contract-freeze" }
state = { kind = "deferred", activation_unit = "P13-A", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p11-hardware"
unit = "P11"
gates = ["G1", "G3", "G5", "G7"]
artifact = { id = "artifact-hardware-matrix", kind = "documentation", path = "docs/testing/storage-hardware-matrix.md" }
command = { id = "cmd-hardware-matrix", kind = "doc-check", argv = ["python3", "scripts/check-storage-hardware-matrix.py", "--report", "docs/testing/storage-hardware-matrix.md"], cwd = ".", path_args = ["scripts/check-storage-hardware-matrix.py", "docs/testing/storage-hardware-matrix.md"], artifact_id = "artifact-hardware-matrix" }
state = { kind = "deferred", activation_unit = "P11", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p12-documentation"
unit = "P12"
gates = ["G6"]
artifact = { id = "artifact-storage-guide", kind = "documentation", path = "docs/storage-ownership.md" }
command = { id = "cmd-storage-guide", kind = "doc-check", argv = ["python3", "scripts/check-storage-docs.py", "--include-rendered"], cwd = ".", path_args = ["scripts/check-storage-docs.py", "docs/storage-ownership.md"], artifact_id = "artifact-storage-guide" }
state = { kind = "deferred", activation_unit = "P12", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p12-element-access-guide"
unit = "P12"
gates = ["G4", "G6"]
artifact = { id = "artifact-element-access-guide", kind = "documentation", path = "docs/guides/views-and-slicing.md" }
command = { id = "cmd-element-access-guide", kind = "doc-check", argv = ["python3", "scripts/check-storage-element-access-docs.py", "docs/guides/views-and-slicing.md"], cwd = ".", path_args = ["scripts/check-storage-element-access-docs.py", "docs/guides/views-and-slicing.md"], artifact_id = "artifact-element-access-guide" }
state = { kind = "deferred", activation_unit = "P12", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p12-element-access-examples"
unit = "P12"
gates = ["G4", "G6"]
artifact = { id = "artifact-element-access-examples", kind = "tutorial", path = "docs/tutorial-code/src/bin/storage_element_access.rs" }
command = { id = "cmd-element-access-examples", kind = "cargo-test", argv = ["cargo", "test", "-p", "tenferro-tutorial-code", "--release", "tutorial_binaries_run_successfully", "--", "--exact"], cwd = ".", path_args = [], artifact_id = "artifact-element-access-examples" }
state = { kind = "deferred", activation_unit = "P12", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."

[[obligations]]
id = "p13-closure"
unit = "P13-B"
gates = ["G1", "G2", "G3", "G4", "G5", "G6", "G7"]
artifact = { id = "artifact-storage-closure", kind = "documentation", path = "docs/worklogs/storage-redesign-closure.md" }
command = { id = "cmd-storage-closure", kind = "source-contract", argv = ["python3", "scripts/check-storage-redesign-closure.py", "--report", "docs/worklogs/storage-redesign-closure.md"], cwd = ".", path_args = ["scripts/check-storage-redesign-closure.py", "docs/worklogs/storage-redesign-closure.md"], artifact_id = "artifact-storage-closure" }
state = { kind = "deferred", activation_unit = "P13-B", promotion = { mode = "activate-in-place" } }
rationale = "The obligation is one immutable artifact-command contract owned by one graph unit."
