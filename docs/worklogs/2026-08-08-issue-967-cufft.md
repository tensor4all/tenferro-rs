# Work log: issue #967 CUDA/cuFFT execution

## Session summary

Issue [#967](https://github.com/tensor4all/tenferro-rs/issues/967) adds CUDA/cuFFT
execution for the existing one-dimensional `fft`, `ifft`, `rfft`, and `irfft`
operations. Public signatures and operation names remain unchanged. Host
execution remains on RustFFT; CUDA execution is explicit for concrete, eager,
and traced paths, with no implicit host/device movement.

Session provenance was the issue, its approved working design, and its approved
implementation plan. Those standalone working artifacts are not linked here
and are excluded from the external PR. This file is the concise durable record;
the eventual PR should link it and issue #967.

## Context and references

Reviewed [work-log conventions](README.md), [review decision records](../design/review-decision-records.md),
[repository rules](../../REPOSITORY_RULES.md), [agent guidance](../../AGENTS.md),
[FFT backend execution](../design/fft-backend-execution.md), [GPU backend design](../design/gpu-backend-design.md),
and the [FFT user guide](../guides/tenferro-fft.md). Consulted NVIDIA's
[cuFFT API](https://docs.nvidia.com/cuda/cufft/index.html) and its
PlanMany64, work-area, stream, destroy, [stream](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__STREAM.html),
and [asynchronous execution](https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html#asynchronous-execution)
contracts, plus FFT/CubeCL interop sources, the cuSOLVER loader precedent, and
eager/runtime extension ownership.

## Implementation history by phase

- **Contract and FFI:** feature wiring, checked rank-one descriptors, dynamic
  loading, typed status mapping, and RAII plan construction.
- **Device execution:** owner-scoped scaling, existing-cache storage, axis
  canonicalization, out-of-place PlanMany64, normalization, and Hermitian completion.
- **Runtime and closure:** exact eager targeting, transactional publication,
  concrete/eager/traced dispatch, cache/retirement/transfer assertions, durable
  design updates, the tutorial, and the user-facing guide.

## Ownership and durable decisions

1. **Placement and API semantics stay explicit.** `tenferro-fft` validates exact
   `CudaRuntime` residency and compact column-major layout. Host tensors and
   tensors from a foreign runtime return typed errors. CUDA code never uploads,
   downloads, constructs a CPU backend, or calls RustFFT.
2. **The FFT crate owns cuFFT.** `tenferro-fft` owns the dynamic `libloading`
   loader, ABI table, descriptors, opaque plans, the `cufft-plans` namespace,
   typed lifecycle errors, and CubeCL workspaces associated with its plans.
   Non-empty `TENFERRO_CUFFT_PATH` entries are tried first, followed by CUDA 12,
   CUDA 11, and bare sonames. The loaded library is retained by each plan;
   there is no process-global or thread-local loader state.
3. **Descriptors make the column-major contract explicit.** After the selected
   axis is moved last, `PlanMany64` uses `istride = ostride = batch` and
   `idist = odist = 1`. Owned non-null rank-one `n`, `inembed`, and `onembed`
   arrays are always passed. C2C uses `[n]/[n]`, R2C `[n]/[n / 2 + 1]`, and C2R
   `[n / 2 + 1]/[n]`. Shape products and conversions to cuFFT integer widths
   are checked before the FFI boundary.
4. **Existing device operations do canonicalization.** CUDA transpose moves
   the requested axis to the final logical axis and the inverse permutation
   restores it. C2C and R2C inputs are sliced or zero-padded on device for an
   explicit `n`; C2R consumes the validated half-spectrum unchanged and uses
   `n` only as the real output length. No custom packing or direct-stride path
   was introduced.
5. **The existing cache remains the owner.** Repeated concrete calls use the
   caller-owned `FftExecutionCache`; eager and traced calls use the owning
   runtime's extension cache. Exact runtime identity, device, dtype/kind,
   direction, length, batch, and stride facts are retained in the key/value, so
   a discriminator collision cannot reuse a different plan. Existing entry and
   byte limits, LRU eviction, clear, stats, and retirement are the only cache
   controls.
6. **Stream binding and retirement are explicit.** Each execution makes the
   retained context current, binds the current CubeCL stream immediately before
   the dtype-specific cuFFT call, and keeps stream and device pointers inside
   scoped callbacks. The current runtime has one serialized current stream
   through the mutable backend-session boundary, so stream identity is omitted
   from the key. If selectable concurrent streams are added, stream identity
   and per-stream workspace ownership must enter the key before reuse.
7. **Retirement favors safety over forced cleanup.** Eviction and clear make the
   retained context current and synchronize its stream before destroying the
   plan and releasing its workspace. Cleanup failures are non-panicking. If
   context activation, synchronization, or destruction cannot be proved safe,
   the complete plan/workspace/library/runtime witness bundle is intentionally
   retained rather than freed while queued work may still reference it.
8. **Scaling is a narrow owner-scoped bridge.** `tenferro-gpu` owns the generic
   `scale_tensor_write` interop helper, reusing structural scale kernels and
   scoped allocation/pointer APIs. cuFFT stays out of place; scaling writes the
   separate output buffer without exposing unrestricted raw pointers.
9. **Real full spectra stay on device.** Real-input `fft` runs R2C/D2Z, applies
   normalization, then uses existing slice, reverse, conjugate, and concatenate
   operations. DC and the even-length Nyquist bin are not duplicated; odd/even
   lengths, including the small lengths one and two, follow the same rule.
10. **Eager selection is owner-selected and transactional.** `tenferro-ad`
    resolves the exact `EngineId` and closed backend kind supplied by the eager
    runtime owner. FFT matches that kind only to its existing CPU/CUDA/WebGPU
    module factory; it does not infer placement or invent an engine ID. Input
    ingress is validated before factory invocation. Proposed module registration
    validates the exact family/engine pair before atomic publication, so a failed
    replacement leaves the prior runtime snapshot intact. The bridge
    then enters the existing non-owning backend session.

## Rejected or deferred alternatives

- CPU/RustFFT fallback, implicit upload/download, and placement-based backend
  inference were rejected: unsupported or misplaced CUDA input must fail at its
  owning boundary.
- In-place real FFT was rejected because cuFFT's padding and overwrite rules do
  not fit the compact column-major contract cleanly; all transforms are out of
  place.
- Custom pack/unpack, fused Hermitian kernels, and shape-dependent direct
  strides were deferred. Existing transpose, slice, pad, reverse, conjugate,
  concatenate, and scale operations are the smallest complete implementation;
  specialized kernels need a measured bottleneck first.
- Hidden backend/global/thread-local caches and process-global loader state were
  rejected in favor of caller/runtime-owned cache and explicit plan lifetime.
- Unrestricted raw stream or pointer exposure was rejected; interop callbacks
  borrow each handle only for the FFI or launch scope.
- Event-based deferred retirement was deferred for the current serialized-stream
  model. It may be reconsidered only if measured cache churn makes synchronized
  retirement material.
- Aggregate extension lookup, placement inference in `tenferro-ad`, and
  non-transactional eager installation were rejected because they can mask a
  mismatch or replace a valid snapshot with a failed replacement.

## User behavior and documentation

The API remains the existing concrete, eager, and traced FFT surface with the
same dtypes, axes, lengths, and normalization modes. CUDA users transfer
explicitly, select a CUDA runtime/session, and can verify device residency before
download. Missing cuFFT, foreign residency, unsupported combinations, and invalid
configuration produce typed errors; a zero non-transform extent returns a
correctly shaped empty CUDA output before library loading or plan creation.

The guide documents CUDA setup, `CUDA_PATH`, `LD_LIBRARY_PATH`,
`CUBECL_DEBUG_LOG`, `TENFERRO_CUFFT_PATH`, explicit transfer boundaries, and
limitations. The runnable tutorial emits an explicit no-device marker or fails
in strict mode; its strict A100 path checks residency, synchronization, and
explicit download. Durable execution and GPU ownership contracts are in the
linked design documents.

## Verification matrix

The recorded hardware run used one NVIDIA A100 80GB PCIe at runtime/device
ordinal 0 with `CUBECL_DEBUG_LOG=0`,
`TENFERRO_CUFFT_PATH=/usr/local/cuda/lib64/libcufft.so.11`,
`CUDA_PATH=/usr/local/cuda`, and
`LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/lib/x86_64-linux-gnu/libcutensor/12:${LD_LIBRARY_PATH:-}`.
The host probe identified CUDA toolkit 12.6 (`nvcc 12.6.85`), cuFFT 11.3.0.4,
and driver CUDA 13.0; the device was compute capability 8.0.

| Area | Exact reproducible command and result | Environment or evidence |
| --- | --- | --- |
| Formatting | `cargo fmt --all -- --check` — passed | Host-runnable |
| CPU FFT | `cargo test -p tenferro-fft --no-default-features --features cpu-faer` — 64 passed | Host-runnable |
| CUDA host contracts | `cargo test -p tenferro-fft --no-default-features --features cpu-faer,cuda --lib cuda::tests -- --nocapture` — 31 passed | No GPU required |
| Feature, check, and lint surface | `cargo check -p tenferro-fft --no-default-features --features autodiff,cpu-faer,cuda,webgpu`; `cargo clippy -p tenferro-fft --no-default-features --features cpu-faer,cuda --all-targets -- -D warnings`; `cargo test -p tenferro-fft --no-default-features --features autodiff,cpu-faer,cuda --test cuda_fft --no-run` — passed | Host-runnable |
| CUDA FFT integration | `CUBECL_DEBUG_LOG=0 TENFERRO_CUFFT_PATH=/usr/local/cuda/lib64/libcufft.so.11 CUDA_PATH=/usr/local/cuda LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/lib/x86_64-linux-gnu/libcutensor/12:${LD_LIBRARY_PATH:-} cargo test -p tenferro-fft --no-default-features --features autodiff,cpu-faer,cuda --test cuda_fft -- --ignored --nocapture` — 24 passed, 2 filtered | The A100 run above; explicit upload/download were the only transfer boundaries |
| A100 `tenferro-gpu` scaling | `CUBECL_DEBUG_LOG=0 CUDA_PATH=/usr/local/cuda LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/lib/x86_64-linux-gnu/libcutensor/12:${LD_LIBRARY_PATH:-} cargo test -p tenferro-gpu --features cuda --lib cubecl::interop::tests::scale -- --ignored --nocapture` — 11 passed, 206 filtered out | Separate `tenferro-gpu` unit-test target on the A100; this is not part of `cuda_fft` |
| A100 eager CUDA target bridge | `CUBECL_DEBUG_LOG=0 CUDA_PATH=/usr/local/cuda LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/lib/x86_64-linux-gnu/libcutensor/12:${LD_LIBRARY_PATH:-} cargo test -p tenferro-ad --no-default-features --features cpu-faer,cuda --lib extension::tests::eager_extension_factory_receives_exact_cuda_target -- --ignored --nocapture` — 1 passed, 78 filtered out | Separate `tenferro-ad` unit-test target on the A100; this is not part of `cuda_fft` |
| A100 coverage | The exact `cuda_fft` command passed C2C/R2C/C2R/full-real transforms for supported dtypes, final/middle/negative axes, batches, explicit lengths, norms, concrete/eager/traced dispatch, cache reuse/limits, immediate clear/eviction, zero-batch, mismatch/errors, and no-transfer assertions | Scaling and eager target bridge results are recorded in their separate rows |
| CUDA provider | `cargo test -p tenferro-gpu --features cuda --lib` — 88 passed, 123 ignored | Host-runnable portion |
| Runtime and AD | `cargo test -p tenferro-runtime --no-default-features --features cpu-faer runtime::tests::extension -- --nocapture` — 16 passed; `cargo test -p tenferro-ad --no-default-features --features cpu-faer` — 560 passed; `cargo test -p tenferro-linalg --no-default-features --features autodiff,cpu-faer --lib` — 162 passed; `cargo test -p tenferro-einsum --no-default-features --features autodiff,cpu-faer --lib eager_ad::tests` — 10 passed | CPU-focused commands |
| Docs and links | `python3 scripts/check-doc-snippets.py --check`; `python3 scripts/check-guide-dependency-snippets.py`; `bash scripts/build_docs_site.sh /tmp/tenferro-rs-docs` — passed | Snippets, guide dependencies, rustdoc, rendered docs, and internal links checked |
| Host source contract | `cargo test -p tenferro-fft --no-default-features --features autodiff,cpu-faer,cuda --lib cuda::tests -- --nocapture` — passed | CUDA production modules contain no upload, download, or host-payload accessor calls |

The simultaneous CUDA/WebGPU feature check passed. Cache-limit checks retained
normal bounded behavior: reducing entry capacity from two to one evicted one
plan, and reducing retained bytes to one byte evicted the remainder. WebGPU
compile checks passed, but no adapter execution was attempted because it is
unrelated to this change. The focused verification does not claim full
workspace coverage, release-mode performance, or the hosted CI matrix; those
remain PR gates.

## Remaining risks and follow-up

- The CUDA runtime serializes one current stream. Selectable concurrent streams
  require per-stream cache identity, workspace ownership, and retirement.
- Cleanup failure intentionally retains the complete witness bundle; this is
  bounded to failure paths and safer than freeing resources with queued work.
- No second GPU or multi-stream path was exercised. WebGPU runtime execution is
  hardware-gated and outside this change.
- A broad autodiff all-target clippy/test invocation still shows four
  pre-existing `tests/fft_ops.rs` calls to `Tensor::clone()`; unrelated cleanup
  is out of scope. Full coverage and hosted CI remain PR integration gates.
