# Issue 1759: shared public-boundary inventory

## Context and decisions

Parent #1758 requires measurement before optimization. This independently
mergeable inventory change establishes disposition and case-selection contracts,
not performance claims. Existing operation/capability/AD authorities remain the
membership sources. Source-only allocations and semantic alias coverage do not
prove timings. Preserve existing APIs and numerical execution unchanged.

Design: [public-boundary-overhead-inventory](../design/public-boundary-overhead-inventory.md).
Baseline: `0457a2ed0aeea21b14f4297f7f4731e09b3a0507`, confirmed with
`git rev-parse HEAD origin/main` before implementation (both matched).

## Review gate

- Design round 1: DeepSeek V4 Flash, read-only, **Correct-to-merge**, before any
  implementation. Reviewed the complete design, core catalog, tensor capability,
  AD manifests, CI/local scripts and extension layout.
- Non-blocking pins: confirmed baseline; chose
  `scripts/check-public-boundary-inventory.py` without copying existing hardcoded
  operation lists; explicitly include standalone sparse/tropical; document that
  operation additions require dispositions. These clarifications are in the design.
- Implementation: assigned to Luna; current worktree implementation is complete for parent review.
- DeepSeek V4 Flash interim review was **not Correct-to-merge**. Findings F1–F5
  were fixed in this continuation: owners are now attached per concrete/eager/
  traced route (test-only `compile_std_to_exec` and traced-AD
  `metadata_scopes_with_new` are no longer misattributed), no-planning routes
  are explicit not-applicable, enum extraction rejects unsupported variants,
  generated snapshot/export were regenerated, and each category has distinct
  regression references. Final Flash re-review remains parent-owned.
- Full-diff review and final integration remain parent-owned; this record does not
  assert completion of #1759 or the parent.

## Verification and remaining evidence

The implementation remains inventory-only: no Rust/public API changes, benchmark
execution, timing collection, or allocation-count claims. Benchmark case execution
remains owned by #95; unsupported hardware and unmeasured paths remain explicit
dispositions, not fabricated measurements. Parent full-diff review and committed-head
repository-rules review must occur after the implementation is accepted and committed.

## Implementation evidence

The maintained overlay is `docs/internals/public-boundary-overhead-inventory.json`; the
Markdown snapshot and benchmark export are generated by
`scripts/check-public-boundary-inventory.py`. Membership is source-derived from the core catalog, linalg and FFT enums, and
sparse/tropical/einsum family authorities discovered from extension declarations.
The maintained map is compact: selectors group reviewed categories instead of
copying the operation registry. Owners reference actual validation, metadata,
planning, session admission, execution, and wrapping symbols; each route has a
route-specific follow-up or unsupported contract. Case refs name existing test
functions, including values-only, output reuse, nested-session, and materialization
coverage where applicable. Benchmark #95 is pending and no timings or allocation
counts are claimed.

Verification previously run before the interim fixes is retained above. Continuation
verification (exact commands):

- `python3 scripts/check-public-boundary-inventory.py --generate --export-benchmarks docs/internals/public-boundary-benchmarks.json` — pass.
- `timeout 30 python3 scripts/test-public-boundary-inventory.py` — pass (source mutation, checker rejection, enum tuple rejection, per-category refs, case selection and benchmark contracts; expected stale-snapshot diagnostic is emitted by the test).
- Local verification commit: `b77fa0d` (`inventory route-specific boundary ownership`).
- `timeout 60 python3 scripts/repository-rules-review.py --base origin/main --head HEAD --dry-run --llm-skipped-reason 'local deterministic review' --output-json /tmp/repository-rules-review.json` — pass; external LLM warning only.

Remaining required gate: parent-owned final Flash re-review. Benchmark execution and
hardware-specific routes remain follow-up work for #95/#96 and the parent issue.

## Continuation: final Flash findings at `b86ed970`

This continuation is limited to the final review corrections; it adds no Rust
runtime or public API code. The extension macro's `execute` parameter label was
replaced by the actual einsum and linalg route declarations. Linalg concrete and
eager routes now record no separate planning owner, while traced planning points
to the production compiler lowering function and traced execution points to the
session executor. Eager allocation, payload, and wrapping references now point
to their definitions in `eager.rs`. `_source_ref` now accepts only anchored
function/struct/trait/enum/type/const/macro declarations (and test refs only
functions), preventing imported names and macro parameter labels from passing.
The mutation tests cover phantom/imported refs and single-line struct enum
variants.

Commands run in this continuation:

- `timeout 30 python3 scripts/test-public-boundary-inventory.py` — RED before
  the checker fix (phantom `execute` accepted), then pass after the fix.
- `timeout 30 python3 scripts/check-public-boundary-inventory.py --generate --export-benchmarks docs/internals/public-boundary-benchmarks.json` — pass.
- `timeout 30 python3 scripts/test-public-boundary-inventory.py` — pass.
- `timeout 30 python3 scripts/check-public-boundary-inventory.py` — pass (`180`
  case contracts across `6` families).
